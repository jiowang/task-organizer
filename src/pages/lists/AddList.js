import React from 'react';
import { useParams } from 'react-router';
import { NavLink } from 'react-router-dom';

const AddList = props => {
    return (
    <React.Fragment>
        <section className="page-section" id="addlist">
            <div className="container masthead">
                <h1>AddList</h1>
            </div>
        </section>
    </React.Fragment>
    );
};

export default AddList;